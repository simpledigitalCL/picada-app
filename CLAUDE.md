# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project overview

Picada App is a gastronomic discovery mobile/web app for the Chilean market. It's a Next.js 15 (App Router) SPA wrapped in Capacitor for Android/iOS, backed by Supabase (PostgreSQL + Auth + Storage), with Claude AI for dish scanning and Leaflet for interactive maps.

## Commands

```bash
npm run dev              # Dev server on port 3002 (not 3000)
npm run build            # Production build
npm run lint             # ESLint

# Android / Capacitor
npm run cap:copy         # Copy build to Android project
npm run cap:sync         # Sync Capacitor plugins + assets
npm run cap:open:android # Open Android Studio
npm run android:sync:emulator  # Point to http://10.0.2.2:3002 (emulator host)
npm run android:sync:prod      # Point to production domain
```

No test suite exists yet. TypeScript checking: `npx tsc --noEmit`.

## Architecture

The app is a **tab-based SPA** — navigation is driven by React state in `app/page.tsx`, not by Next.js Router. This makes it work as a PWA and inside the Capacitor WebView without complex browser history.

```
app/page.tsx       ← SPA root: manages tabs, auth, modals, gamification init
app/page.tsx          ← SPA root: manages tabs, auth, modals, gamification init
app/api/**            ← Server-side API Routes (all use Supabase service role)
app/public-profile/** ← Public shareable user profile page
components/           ← UI grouped by domain
lib/                  ← Business logic grouped by domain
supabase/             ← schema.sql (idempotent) + migrations/ (apply in date order)
feedie/               ← Standalone prototype; NOT part of the main build
android/              ← Generated by Capacitor; do not edit manually
```


| File | Purpose |
|---|---|
| `app/page.tsx` | SPA orchestrator — most important file |
| `lib/supabase.ts` | Supabase browser client (anon key) |
| `lib/supabase-server.ts` | Supabase server/admin client (service role key) |
| `lib/stores/app-store.ts` | Single Zustand store for global UI state |
| `lib/gamification/core.ts` | XP, levels, streaks logic (client-side) |
| `lib/gamification/achievement-engine.ts` | Achievement detection engine |
| `lib/places/category-filter.ts` | Maps raw Google Maps categories → normalized `local_slug` |
| `lib/places/match.ts` | 0–100 match score between a user profile and a place |
| `lib/feed/personalization.ts` | Feed personalization + localStorage profile settings |
| `lib/server/content-persistence.ts` | All post/media creation logic (server-side) |
| `lib/server/rate-limit.ts` | In-memory rate limiter — every API route must use it |
| `lib/server/auth.ts` | `requireAuthenticatedUser()` — Bearer token → Supabase `getUser()` |

### Supabase clients

- Use `lib/supabase.ts` in browser/client components.
- Use `lib/supabase-server.ts` in API Routes — uses service role key to bypass RLS.
- All RLS policies are in `supabase/schema.sql`. Public reads on `places` and `posts`; writes require `auth.uid() = user_id`.

### API Routes pattern

Each route lives in `app/api/<domain>/route.ts`. Every route must:
1. Call `consumeRateLimit(key, limit, windowMs)` from `lib/server/rate-limit.ts` first.
2. Call `requireAuthenticatedUser(req)` from `lib/server/auth.ts` for write endpoints.
3. Use the admin Supabase client (`lib/supabase-server.ts`).
4. Validate with Zod schemas from `lib/validation/`.

### Post submission flow

The post form (`components/post-form.tsx`) is multi-step:
1. `usePostFormFlow` manages step state and `formAccumulator`.
2. On submit → `usePostFormSubmit.submit()` → `POST /api/posts`.
3. `/api/posts` overrides `body.user.id` with the authenticated user's real Supabase UUID (not the anonymous identity from localStorage).
4. `persistUnifiedContent()` in `lib/server/content-persistence.ts` inserts into `posts` table and `post_media` table.
5. Media URL is stored in `posts.nutrition_data.original_payload.media.url` (the JSONB column) — the `/api/social-feed` route reads it from there, not from a dedicated column.
6. After success, `post-form.tsx` saves the post to `localStorage` (`picada.profile.social.v1.socialPosts`) and dispatches both `picada:review-published` and `picada:post-published` events.

### Cross-component communication (DOM events)

Components communicate via `window.dispatchEvent`. Key events:

| Event | Payload | Who listens |
|---|---|---|
| `picada:post-published` | none | `SocialCommunityFeed` (refreshes remote) |
| `picada:review-published` | `CustomEvent<LocalPost>` | `ProfileView`, `SocialCommunityFeed` (adds to local state) |
| `picada:prefs-updated` | none | Feed components |
| `picada:require-auth` | none | `app/page.tsx` (opens login modal) |
| `picada:xp-granted` | `{ amount, currentTotal }` | XP notification toast |

### User identity

- **Anonymous users** get a localStorage identity from `lib/auth/identity.ts` (`picada.user.id.v1` = `user-xxxxxxxx`, not a UUID).
- **On login**, `app/page.tsx` syncs the real Supabase UUID to localStorage via `lib/auth/sync-profile.ts`.
- **API routes** always use `requireAuthenticatedUser(req)` which returns the real Supabase auth UUID. The `/api/posts` route overwrites `body.user.id` with this UUID before persisting.

### Place IDs: Google external IDs vs internal UUIDs

Places from Google Maps have IDs like `ChIJba...` (not UUIDs). When working with place IDs:
- Check with `normalizeUuid()` — returns `null` for Google IDs.
- If null, query `places` table by `.eq('external_id', googlePlaceId)` to get the internal UUID.
- This pattern is in `lib/server/content-persistence.ts` and `components/post-form/steps/LocationStep.tsx`.

### Tag system (core of personalization)

Tags connect users to places via affinity scores. Three layers:
1. **Classification** (`lib/places/category-filter.ts`) — raw Google Maps categories → `local_slug`
2. **Suggestion scoring** — curated base tags + community frequency (`tag_relations_stats`) + user affinity (`user_tag_affinity`), combined via Supabase RPC at `/api/suggestions`
3. **Affinity accumulation** — `user_tag_affinity` updated by reviews (+3), visits (+2), likes (+1), searches (+1), profile setup (+5)

### Gamification

- XP events emitted client-side via `lib/gamification/events.ts`, persisted server-side via `/api/events`.
- Achievement engine (`lib/gamification/achievement-engine.ts`) triggers toasts via `components/gamification/AchievementToast.tsx`.
- Leaderboard served by `/api/leaderboard`.

### AI integrations

- **Dish scanner** (`/api/scanner`): receives base64 image, calls Anthropic Claude Vision, returns dish name, nutrition estimate, and suggested tags.
- **Auto-tagging** (`lib/places/auto-tagging.ts`): triggered in background when a new place is registered; runs inference via `lib/inference/tag-processor.ts`.

## Environment variables

Copy `.env.example` to `.env.local`:

| Variable | Description |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase project URL (browser) |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase anon key (browser) |
| `SUPABASE_URL` | Supabase project URL (server) |
| `SUPABASE_SERVICE_ROLE_KEY` | Service role key — never expose to client |
| `GOOGLE_MAPS_API_KEY` | Google Maps Platform (Places API New + Geocoding API) |
| `ANTHROPIC_API_KEY` | Anthropic Claude API |

## Conventions

- **Package manager**: npm only (`package-lock.json` — do not use pnpm or yarn).
- **Brand color**: `orange-500` (#f97316) — active tabs, primary actions.
- **HTML sanitization**: all user-generated content passes through `isomorphic-dompurify` (`lib/utils/sanitize.ts`) before rendering.
- **Mobile strategy**: Next.js + Capacitor only. No React Native / Flutter migration planned.
- **Capacitor**: `next.config.mjs` has `images.unoptimized: true` — required for Android WebView.
- `feedie/` is an isolated prototype with its own `package.json` — never import from the main app.

## UI patterns

- **Modals/sheets**: always use `side="bottom"` with `h-[92dvh] rounded-t-3xl p-0 flex flex-col overflow-hidden`. Never use `side="right"` for complex content — it creates a narrow sidebar that clips on mobile.
- **ScrollArea overflow**: Radix UI injects `display: table; min-width: 100%` via inline style on the inner content wrapper, which bypasses CSS `overflow: hidden`. The fix (already applied in `components/ui/scroll-area.tsx`) wraps children in `<div className="w-full">`. Do not remove this wrapper.
- **shadcn/ui components**: do not manually edit files in `components/ui/` except for established fixes (the ScrollArea wrapper above, the `aria-describedby={undefined}` on Sheet).
- **`CardContent` padding**: default is `px-6`. In dense layouts (grids, cards inside sheets) override with `px-3` or `px-4` to prevent horizontal overflow on narrow viewports.

## Critical gotchas

**`PostgrestFilterBuilder` is not a native Promise** — do not use `.catch()`. Use `.then(undefined, () => undefined)` for fire-and-forget background tasks:
```ts
// ✗ TypeError at runtime
supabase.from('table').insert({...}).catch(() => undefined)

// ✓ Correct
supabase.from('table').insert({...}).then(undefined, () => undefined) as Promise<unknown>
```

**`console.error()` in dev triggers Next.js error overlay** — use `console.log()` wrapped in `if (process.env.NODE_ENV === 'development')` for debug logging.

**`getSupabaseServerClient()` returns `null` if `SUPABASE_SERVICE_ROLE_KEY` is missing** — always null-check the result in API routes.

## Database

Apply schema to a Supabase project via the SQL Editor:
1. Run `supabase/schema.sql` (idempotent).
2. Run each file in `supabase/migrations/` in chronological order.

Required PostgreSQL extensions: `pgcrypto`, `pg_trgm`.

Key tables not in schema.sql (created directly in Supabase): `posts`, `post_media`, `profiles`, `user_points`, `follows`, `saved_items`.
